import 'package:flutter/material.dart';

class CourDetails extends StatelessWidget {
  const CourDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [
          
        ],
      ),
    );
  }
}
